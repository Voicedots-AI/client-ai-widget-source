/** Injected at build time from clients/<name>.json → `forceEmbed`. Empty for
 *  a plain `npm run build`. Overrides the config on the client's own page. */
declare const __VD_FORCED_CONFIG__: Record<string, unknown>;
